apiVersion: v1
kind: ServiceAccount
metadata:
  name: kie-server-restart-tool-use-policy
  namespace: drools
  annotations:
    argocd.argoproj.io/sync-wave: "29"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kie-server-restart-tool-use-policy
  namespace: drools
  annotations:
    argocd.argoproj.io/sync-wave: "29"
rules:
  - apiGroups: ["apps"]
    resources: ["statefulsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["statefulsets"]
    resourceNames: ["kie-server"]
    verbs: ["patch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kie-server-restart-tool-use-policy
  namespace: drools
  annotations:
    argocd.argoproj.io/sync-wave: "29"
subjects:
  - kind: ServiceAccount
    name: kie-server-restart-tool-use-policy
    namespace: drools
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kie-server-restart-tool-use-policy
