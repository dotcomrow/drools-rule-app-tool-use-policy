# Generated by scripts/generate-settings-doc.py
# This is a full reference template; it is not included in kustomization.yaml.

apiVersion: v1
kind: ConfigMap
metadata:
  name: register-container-config-tool-use-policy
  namespace: drools
  annotations:
    argocd.argoproj.io/sync-wave: "10"
data:
  # Required. Workbench controller REST base URL.
  # Default: "http://kie-workbench.drools.svc.cluster.local:8080/business-central/rest/controller" (source: config)
  WORKBENCH_CONTROLLER_URL: "http://kie-workbench.drools.svc.cluster.local:8080/business-central/rest/controller"
  # Optional. Git URL used to resolve the container version when VERSION is empty/auto.
  # Default: "https://github.com/dotcomrow/drools-rule-app-tool-use-policy.git" (source: config)
  REPO_URL: "https://github.com/dotcomrow/drools-rule-app-tool-use-policy.git"
  # Optional. Preferred branch to resolve the container version from (falls back to prod/main/master).
  # Default: "prod" (source: config)
  REPO_BRANCH: "prod"
  # Optional. Maven repository base URL used to verify published versions.
  # Default: "https://maven.pkg.github.com/dotcomrow/drools-rule-app-tool-use-policy" (source: config)
  MAVEN_REPO_URL: "https://maven.pkg.github.com/dotcomrow/drools-rule-app-tool-use-policy"
  # Optional. KIE server id to register with (required if KIE_SERVER_IDS is empty).
  # Default: "auto" (source: config)
  KIE_SERVER_ID: "auto"
  # Optional. Comma-separated list of KIE server ids to register with.
  # Default: empty (source: job)
  KIE_SERVER_IDS: ""
  # Optional. Prefix used when selecting auto KIE server ids.
  # Default: "kie-server-" (source: config)
  KIE_SERVER_ID_PREFIX: "kie-server-"
  # Optional. Minimum number of KIE servers that must be available for auto selection.
  # Default: "2" (source: config)
  KIE_SERVER_MIN_COUNT: "2"
  # Required. Container id to register on the KIE server.
  # Default: "tool-use-policy" (source: config)
  CONTAINER_ID: "tool-use-policy"
  # Optional. Container display name (defaults to CONTAINER_ID if empty).
  # Default: "$CONTAINER_ID" (source: job)
  CONTAINER_NAME: ""
  # Required. Maven groupId for the KJAR.
  # Default: "com.dotcomrow.rules" (source: config)
  GROUP_ID: "com.dotcomrow.rules"
  # Required. Maven artifactId for the KJAR.
  # Default: "tool-use-policy" (source: config)
  ARTIFACT_ID: "tool-use-policy"
  # Optional. KJAR version to register in the container spec. If empty or 'auto', derived from the repo.
  # Default: "1.0.153" (source: config)
  VERSION: "1.0.153"
  # Optional. Prefix used when deriving a version from git history (e.g. 1.0.). Leave empty to infer from pom.
  # Default: empty (source: config)
  VERSION_PREFIX: ""
  # Optional. If true, fail registration when the desired version is not published in the Maven repo.
  # Default: "true" (source: config)
  REQUIRE_PUBLISHED_VERSION: "true"
  # Optional. If true, use the latest published version when the desired version is unavailable.
  # Default: "false" (source: config)
  FALLBACK_TO_LATEST_PUBLISHED: "false"
  # Optional. Seconds to wait for the desired VERSION to appear in Maven metadata before failing/falling back.
  # Default: "1200" (source: config)
  PUBLISHED_VERSION_WAIT_SECONDS: "1200"
  # Optional. Seconds between Maven metadata checks while waiting for the desired VERSION to publish.
  # Default: "15" (source: config)
  PUBLISHED_VERSION_POLL_SECONDS: "15"
  # Optional. If true, delete existing container specs when versions/status differ before re-registering.
  # Default: "true" (source: config)
  CLEANUP_OLD_VERSIONS: "true"
  # Optional. If true, delete and re-register even when the container already matches the desired version.
  # Default: "false" (source: config)
  FORCE_REDEPLOY: "false"
  # Optional. Container status after registration (e.g., STARTED or STOPPED).
  # Default: "STARTED" (source: config)
  CONTAINER_STATUS: "STARTED"
  # Optional. If true, deploy/update the container directly on the KIE server(s) via KIE Server REST (workaround when controller-based install is blocked).
  # Default: "true" (source: config)
  DEPLOY_TO_KIE_SERVER_REST: "true"
  # Optional. Base URL template for KIE Server REST. Use '{id}' placeholder for the server id.
  # Default: empty (source: config)
  KIE_SERVER_REST_BASE_URL_TEMPLATE: ""
  # Optional. Scheme used when composing KIE Server REST base URL (http or https).
  # Default: "http" (source: config)
  KIE_SERVER_REST_SCHEME: "http"
  # Optional. Headless service name used when composing per-pod KIE Server REST URL.
  # Default: "kie-server-headless" (source: config)
  KIE_SERVER_REST_HEADLESS_SERVICE: "kie-server-headless"
  # Optional. Namespace used when composing per-pod KIE Server REST URL.
  # Default: "drools" (source: config)
  KIE_SERVER_REST_NAMESPACE: "drools"
  # Optional. Port used when composing per-pod KIE Server REST URL.
  # Default: "8080" (source: config)
  KIE_SERVER_REST_PORT: "8080"
  # Optional. Context path for KIE Server REST base (default: /kie-server/services/rest/server).
  # Default: "/kie-server/services/rest/server" (source: config)
  KIE_SERVER_REST_CONTEXT_PATH: "/kie-server/services/rest/server"
  # Optional. Vault KV path containing username/password used for KIE Server REST calls.
  # Default: "secret/drools/admin" (source: config)
  KIE_SERVER_REST_CREDS_PATH: "secret/drools/admin"
  # Optional. Max seconds to wait for the KIE server REST endpoint to become available.
  # Default: "900" (source: config)
  KIE_SERVER_REST_WAIT_SECONDS: "900"
  # Optional. Seconds between KIE server REST availability checks.
  # Default: "5" (source: config)
  KIE_SERVER_REST_WAIT_INTERVAL: "5"
  # Optional. Seconds to subtract from token expiry before refreshing.
  # Default: "30" (source: job)
  TOKEN_REFRESH_SKEW: "30"
  # Optional. If true, log HTTP interactions with the Workbench controller.
  # Default: "true" (source: config)
  LOG_HTTP: "true"
  # Optional. If true, only log non-2xx/3xx HTTP responses.
  # Default: "false" (source: config)
  LOG_HTTP_ONLY_ERRORS: "false"
  # Optional. Max bytes of HTTP body to log.
  # Default: "8000" (source: config)
  LOG_HTTP_BODY_MAX: "8000"
  # Optional. Max seconds to wait for Workbench controller availability.
  # Default: "900" (source: job)
  CONTROLLER_WAIT_SECONDS: "900"
  # Optional. Seconds between controller availability checks.
  # Default: "5" (source: job)
  CONTROLLER_WAIT_INTERVAL: "5"
  # Optional. Max seconds to wait for KIE server availability.
  # Default: "900" (source: job)
  KIE_SERVER_WAIT_SECONDS: "900"
  # Optional. Seconds between KIE server availability checks.
  # Default: "5" (source: job)
  KIE_SERVER_WAIT_INTERVAL: "5"
