# Generated by scripts/generate-settings-doc.py
# This is a full reference template; it is not included in kustomization.yaml.

apiVersion: v1
kind: ConfigMap
metadata:
  name: workbench-import-config-tool-use-policy
  namespace: drools
  annotations:
    argocd.argoproj.io/sync-wave: "10"
data:
  # Required. Base URL for Workbench (business-central).
  # Default: "http://kie-workbench.drools.svc.cluster.local:8080/business-central" (source: config)
  WORKBENCH_BASE: "http://kie-workbench.drools.svc.cluster.local:8080/business-central"
  # Required. Workbench space name to create/use.
  # Default: "rules" (source: config)
  SPACE_NAME: "rules"
  # Required. Workbench space description.
  # Default: "Rules" (source: config)
  SPACE_DESC: "Rules"
  # Required. Default groupId for projects created in the space.
  # Default: "com.dotcomrow.rules" (source: config)
  DEFAULT_GROUP_ID: "com.dotcomrow.rules"
  # Required. Workbench project/repo name (Niogit repo name).
  # Default: "tool-use-policy" (source: config)
  REPO_NAME: "tool-use-policy"
  # Required. Git URL used to clone the project into Workbench.
  # Default: "https://github.com/dotcomrow/drools-rule-app-tool-use-policy.git" (source: config)
  REPO_URL: "https://github.com/dotcomrow/drools-rule-app-tool-use-policy.git"
  # Optional. Preferred branch to set as HEAD (falls back to prod/main/master).
  # Default: "prod" (source: config)
  REPO_BRANCH: "prod"
  # Optional. Version to stamp into pom.xml after import. If empty or 'auto', derived from the repo.
  # Default: "1.0.175" (source: config)
  REPO_VERSION: "1.0.175"
  # Optional. Prefix used when deriving a version from git history (e.g. 1.0.). Leave empty to infer from pom.
  # Default: empty (source: config)
  VERSION_PREFIX: ""
  # Optional. Max seconds to wait for async space creation to complete.
  # Default: "300" (source: config)
  SPACE_WAIT_SECONDS: "300"
  # Optional. Owner for the space. Leave empty to auto-pick controller user or service account.
  # Default: empty (source: config)
  SPACE_OWNER: ""
  # Optional. If true, keep the import pod running on failure for debugging.
  # Default: "false" (source: config)
  HOLD_POD: "false"
  # Optional. If true, log REST responses for Workbench API calls.
  # Default: "true" (source: config)
  DEBUG_REST: "true"
